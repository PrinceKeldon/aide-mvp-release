version: "3.8"

services:
  searxng:
    image: searxng/searxng:latest
    container_name: aide-searxng
    ports:
      - "8080:8080"
    volumes:
      - ./docker/searxng:/etc/searxng:rw
    environment:
      - SEARXNG_BASE_URL=http://localhost:8080
    restart: unless-stopped
